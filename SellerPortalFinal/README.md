# Seller Onboarding Portal

## Features
- Multi-step seller onboarding
- Styled PDF generation
- SMTP email integration (Office365)
- TailwindCSS branding

## Setup
1. Copy `.env.local.example` to `.env.local` and set credentials.
2. Run `npm install`
3. Run `npm run dev`
4. Deploy via Vercel or similar
